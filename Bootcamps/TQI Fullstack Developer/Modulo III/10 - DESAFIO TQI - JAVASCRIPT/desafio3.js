let imput = gets()

let qtdEntradas = parseInt(imput);
let melhor = 999;

for(let i = 0, i < qtdEntradas; i++) {
    imput - gets()
    let tempo = parseFloat(imput);
    if(tempo < melhor) {
        melhor = tempo};
}

print(melhor)